import { type ChartConfig } from "@/components/ui/chart"

export const chartConfig = {
    monthlySold: {
        label: "Amount",
        color: "hsl(142 88% 28%)",
    },
    views: {
        label: "Views",
        color: "hsl(139 65% 20%)",
    }
} satisfies ChartConfig